import React from 'react';

const TransparentButton = (props) => {
    return (
        <div className="transparentButton">
            <button>{props.transparentButton}</button>
        </div>
    );
}

export default TransparentButton;